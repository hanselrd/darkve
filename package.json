{
  "name": "darkve",
  "version": "1.0.0",
  "description": "",
  "main": "server/build/index.js",
  "scripts": {
    "start": "cd server && npm start",
    "clean": "rm -rf build",
    "build:common": "cd common && npm run build",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "build": "run-s clean build:*",
    "preinstall:common": "cd common && npm install",
    "preinstall:client": "cd client && npm install",
    "preinstall:server": "cd server && npm install",
    "preinstall": "run-s preinstall:*",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "cd server && npm start"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/hanselrd/darkve.git"
  },
  "author": "Hansel De La Cruz",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/hanselrd/darkve/issues"
  },
  "homepage": "https://github.com/hanselrd/darkve#readme",
  "dependencies": {
    "npm-run-all": "^4.1.2"
  }
}
